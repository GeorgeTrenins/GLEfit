# merit_function references non-existent dataset
project:
  name: "test"
  output_dir: "./output"

data:
  kernel_data:
    source: "inline"
    grid: [0.0, 0.5, 1.0]
    target: [1.0, 0.5, 0.1]

merit_function:
  property: "MemoryKernel"
  data_ref: "nonexistent_data"
  weight: 1.0
  metric: "squared"

embedder:
  type: "PronyEmbedder"

optimization:
  optimizer: "NewtonRaphson"
  max_steps: 100
