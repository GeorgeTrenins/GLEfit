# missing embedder.type
project:
  name: "test"
  output_dir: "./output"

data:
  test_data:
    source: "inline"
    grid: [0.0, 0.5, 1.0]
    target: [1.0, 0.5, 0.1]

merit_function:
  property: "MemoryKernel"
  data_ref: "test_data"

embedder:
  parameters:
    theta: 1.0

optimization:
  optimizer: "NewtonRaphson"
  max_steps: 100
